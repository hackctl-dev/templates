# PostgreSQL connection string for local development.
DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/hackctl_pern

# Backend service port. Must match hackctl.config.json backend port.
PORT=5000
